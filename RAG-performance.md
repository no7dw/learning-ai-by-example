# 企业级 RAG 全链路耗时优化

## 进阶课程：从检索正确到稳定、快速地返回答案

本课程是 [`rag-vector-database-course.md`](rag-vector-database-course.md) 的进阶部分。
基础课程解决的是“如何把文档变成向量并检索出来”，本课程解决的是另一个生产问题：

> 在不牺牲权限、相关性和答案可信度的前提下，让 RAG 更快地返回结果，并且在高并发下保持稳定。

课程围绕六层优化展开：Embedding、检索、排序、LLM 生成、系统架构，以及针对高频和明确意图查询的预打分快速通道。

## 1. 学习目标

完成本课程后，你应该能够：

1. 把一次 RAG 请求拆成可测量的延迟阶段，并识别 p50、p95、p99 的差异。
2. 判断一个慢请求主要受 Embedding、向量检索、Rerank、LLM 首 token 还是网络影响。
3. 使用 GPU 推理、批处理、Embedding 缓存和答案缓存降低重复计算。
4. 调整 HNSW、预加载、元数据过滤和 Top-K，在召回质量与延迟之间做实验。
5. 用轻量级 Reranker、异步并发和必要时跳过 Rerank 的策略控制排序成本。
6. 通过限制上下文长度、流式输出和推理服务优化生成阶段。
7. 设计多级缓存、分片和水平扩容，同时处理缓存一致性与权限边界。
8. 实现一个受约束的预打分快速通道，并在证据不足时回退到完整 RAG。
9. 用 Recall@K、MRR、Faithfulness、TTFT、吞吐量和成本共同判断优化是否成功。

## 2. 性能优化的核心心智模型

RAG 的总耗时不是一个单独的“向量库耗时”，而是多个串联和并行阶段的总和：

```text
请求
  -> 请求解析与权限上下文
  -> 查询缓存检查
  -> Query Embedding
  -> 元数据过滤
  -> 向量检索
  -> 候选合并与去重
  -> 可选 Rerank
  -> Prompt 组装
  -> LLM 首 token
  -> 流式输出完整答案
```

可以先用下面的近似公式建立延迟预算：

```text
T_total = T_network
        + T_cache_check
        + T_embedding
        + T_filter_and_search
        + T_rerank
        + T_prompt
        + T_model_TTFT
        + T_generation
```

其中：

- `TTFT`（Time To First Token）是模型返回第一个 token 的时间，决定用户是否感觉“开始有响应”。
- `T_generation` 是从首 token 到完整答案的时间，通常受输出 token 数和生成速度影响。
- 流式输出主要改善感知延迟，不会自动减少模型生成全部 token 所需的计算量。

### 2.1 先测量，再优化

一次请求至少要记录以下字段，并使用同一个 `trace_id` 关联：

| 阶段 | 必须记录的字段 | 用途 |
|---|---|---|
| 请求 | `trace_id`、租户、查询类型、时间戳 | 关联完整链路和权限上下文 |
| 缓存 | key 类型、命中/未命中、耗时 | 判断是否重复计算过多 |
| Embedding | 模型、批次、输入 token 数、耗时、错误 | 判断模型服务是否成为瓶颈 |
| 检索 | Collection、过滤条件、`ef`、候选数、耗时 | 判断向量库和过滤性能 |
| 排序 | 模型、输入候选数、批次、耗时 | 判断 Rerank 是否值得保留 |
| Prompt | 最终 token 数、上下文来源、耗时 | 判断上下文是否过长 |
| 生成 | TTFT、输出 token 数、tokens/s、总耗时 | 判断用户感知和生成成本 |
| 质量 | Recall@K、引用命中、拒答、用户反馈 | 防止只优化速度而损失答案质量 |

不要只看平均耗时。平均值可能掩盖偶发的长尾；企业服务至少应该同时观察 p50、p95 和 p99。

### 2.2 用瀑布图定位瓶颈

```text
0ms       20ms       60ms       120ms      300ms       800ms
|---------|----------|----------|----------|-----------|
| cache   | embedding| search   | rerank   | LLM TTFT  |
|         |          |          |          | generation ->
```

一次优化只改变一个主要变量，例如只调整 `ef`、只减少 Top-K，或只替换 Embedding 服务。
否则即使总耗时变快，也无法知道是哪一项带来了变化。

## 3. 六层优化总览

| 层级 | 主要目标 | 常用手段 | 主要风险 |
|---|---|---|---|
| Embedding | 减少查询向量生成时间 | 本地 GPU、TensorRT、vLLM、ONNX、向量缓存 | 模型不兼容、缓存过期 |
| 检索 | 更快缩小候选集合 | HNSW、预加载、元数据过滤、调优 K | Recall 下降、权限过滤错误 |
| 排序 | 用较低成本选出真正相关内容 | 小模型、微调、Batch、异步、跳过 Rerank | 排序质量下降、并发放大 |
| LLM 生成 | 更快开始并完成回答 | 限制上下文、推理加速、流式、答案缓存 | 截断证据、缓存错答 |
| 架构 | 提高吞吐并控制长尾 | 分片、扩容、多级缓存、内存热数据 | 一致性、资源成本、故障复杂度 |
| 快速通道 | 让可预测查询绕过完整链路 | 高频问答、关键词路由、预计算特征 | 误命中、过期答案、绕过权限 |

优化顺序通常应该是：先建立观测，再消除重复计算，再缩小候选集合，最后调模型和基础设施。

## 4. 第一层：Embedding 优化

Embedding 阶段的任务是把查询转换为向量。它通常是 RAG 的第一个远程或模型调用，也是很适合做缓存的阶段。

### 4.1 本地部署与 GPU 推理

当查询量大、网络往返明显，或者外部 Embedding API 的吞吐和限流不满足要求时，可以考虑本地部署 Embedding 模型并使用 GPU 推理。

常见加速路径包括：

- **TensorRT**：把模型转换为更适合 NVIDIA GPU 执行的推理图，适合稳定的模型和硬件环境。
- **vLLM**：主要用于生成模型，也可以在适合的模型与服务封装下提供高吞吐推理能力。
- **ONNX Runtime**：将模型导出为 ONNX，便于使用不同硬件后端和图优化。

工具选择要以实际基准为准。必须同时记录单请求延迟、批量吞吐、GPU 利用率、显存占用和错误率。

### 4.2 Embedding 批处理

索引阶段通常可以批量处理多个文本块。在线查询如果支持批量搜索，也可以将多个查询合并生成向量，但不要为了批处理而人为等待单个用户请求。

```python
async def embed_queries(queries: list[str], batch_size: int = 32) -> list[list[float]]:
    vectors = []
    for start in range(0, len(queries), batch_size):
        batch = queries[start : start + batch_size]
        vectors.extend(await embedding_model.embed_batch(batch))
    return vectors
```

在线服务需要设置批处理等待上限：批次没有在很短时间内填满时立即发送，避免吞吐提高却让单请求 TTFT 变差。

### 4.3 高频查询的向量缓存

可以对规范化后的查询建立向量缓存：

```text
原始 query
  -> 去除无意义空白
  -> 统一大小写或 Unicode 形式
  -> 生成 cache key
  -> 查询 Embedding Cache
       | 命中：直接复用向量
       | 未命中：调用 Embedding，写入缓存
```

缓存 key 至少应包含：

- 规范化后的 query；
- Embedding 模型版本；
- 向量名称或索引版本；
- 语言或租户隔离信息（如果不同租户使用不同模型或数据边界）。

如果修改了 Embedding 模型、维度、距离函数或文本规范化规则，应让缓存自然失效或切换到新的命名空间。

### 4.4 Embedding 层的质量门禁

速度优化不能破坏 Embedding 契约。每次替换模型或推理后端，都应验证：

1. 文档向量和查询向量使用兼容的模型。
2. 向量维度和 Named Vector 名称正确。
3. Cosine、Dot Product 或 Euclidean 的配置没有被意外改变。
4. 离线评估集的 Recall@K 没有超过允许的下降阈值。
5. GPU、CPU 和远程服务对同一批输入的结果差异处于可接受范围内。

## 5. 第二层：检索优化

向量检索阶段的核心目标不是“返回越多越好”，而是在权限和召回质量不变或可接受的范围内，尽快得到一个足够小的候选集合。

### 5.1 调整 HNSW 的 `ef`

HNSW 是常见的近似最近邻索引。`ef` 可以理解为搜索时维护的候选范围大小：

- `ef` 较小：通常更快，但可能漏掉真正相关的向量。
- `ef` 较大：通常 Recall 更高，但 CPU、内存和延迟会增加。

不要直接采用某个固定值。用固定评估集做一组实验：

| `ef` | p95 检索延迟 | Recall@K | Qdrant CPU | 备注 |
|---:|---:|---:|---:|---|
| 32 |  |  |  |  |
| 64 |  |  |  |  |
| 128 |  |  |  |  |
| 256 |  |  |  |  |

选点时要看 Pareto 前沿：如果 `ef` 翻倍只带来很小的 Recall 提升，却显著增加长尾延迟，就不一定值得。

### 5.2 预加载与热数据

向量库启动后，如果索引和热点 Payload 仍然频繁从磁盘读取，首次访问和长尾延迟会明显增加。可以使用预加载、操作系统页缓存或内存型部署，让热数据尽可能留在内存中。

但“热数据放内存”不等于“所有数据都不落盘”：

- 持久化仍然是故障恢复和重启后的数据来源。
- 需要明确内存预算，避免索引和 Payload 挤压业务进程。
- 预加载会增加启动时间，应配合 readiness 检查，避免服务尚未热好就接收流量。

### 5.3 元数据提前过滤

权限、部门、时间、文档类型等过滤条件越早应用，候选集合越小，后续排序和 Prompt 组装越便宜。

```text
用户 query
  -> 租户/用户权限条件
  -> 部门和文档类型
  -> 时间范围
  -> 向量近邻搜索
  -> 返回候选
```

过滤字段应建立 Payload 索引，并确认过滤发生在向量库查询阶段，而不是把所有结果取回应用层后再过滤。

权限过滤必须是强约束。不能因为快速或缓存而跳过租户、用户、部门或文档可见性条件。

### 5.4 找到最优召回数 K

K 同时影响三件事：检索时间、Rerank 输入大小和 LLM 上下文长度。K 过小会漏掉证据，K 过大则会造成排序、Prompt 和生成成本上升。

建议分开记录两个参数：

- `candidate_k`：向量检索返回多少候选给 Reranker。
- `context_k`：最终送入 LLM 的证据数量。

常见流程是先用较大的 `candidate_k` 保证 Recall，再用轻量级 Rerank 缩小为较小的 `context_k`。

```text
candidate_k = 30
    -> 去重、权限校验、Rerank
    -> context_k = 5
    -> Prompt
```

不要把 `candidate_k` 和 `context_k` 混为一个参数。它们服务于不同的质量和性能目标。

### 5.5 过滤与检索的实验矩阵

至少比较以下组合：

1. 无元数据过滤与有元数据过滤。
2. 不同 HNSW `ef`。
3. 不同 `candidate_k`。
4. 是否开启预加载。
5. 单租户热数据与多租户混合数据。

每组实验同时记录 Recall@K、p95 检索延迟、总延迟和错误率。只看向量库的 benchmark 结果，不能代表带权限过滤的真实请求。

## 6. 第三层：排序优化

Reranker 会重新判断 query 与候选文本块之间的相关性，通常能提升最终上下文质量，但也会增加一个模型推理阶段。

### 6.1 优先选择足够小的模型

如果向量检索已经能给出质量稳定的候选，Reranker 不需要使用最大的模型。可以按以下顺序尝试：

1. 不使用 Rerank，建立质量与延迟基线。
2. 使用轻量级 Cross-Encoder 或领域小模型。
3. 只有在评估集明确显示收益时，才使用更大的模型。

Rerank 的收益应该用最终任务指标衡量，而不是只看排序分数变化：

```text
Rerank 收益 = 相关证据进入 context_k 的比例提升
           - 增加的延迟、成本和资源占用
```

### 6.2 领域数据微调

如果通用 Reranker 对企业内部术语、产品型号、表格字段或部门缩写判断不准，可以使用领域标注数据微调。

训练数据应包含难负例，例如：

- 词面相似但权限不同的文档；
- 同一个产品不同版本的文档；
- 同一个标题但答案字段不同的文本块；
- 相关主题但不能回答当前问题的文本块。

微调后必须同时回归质量、延迟、显存和吞吐，避免模型效果提升但在线容量下降。

### 6.3 Batch 与异步

Rerank 通常可以将同一个 query 的多个候选组成一个 batch。多个独立 query 也可以通过微批处理提升吞吐。

```text
query A -> candidates A --+
query B -> candidates B --+-> batch rerank -> 按 query 拆回结果
query C -> candidates C --+
```

注意保持输入 query 与返回结果的对应关系。异步并发也要设置上限，否则 Rerank 请求会同时放大 GPU 显存、队列等待和下游超时。

### 6.4 什么时候可以跳过 Rerank

满足以下条件时，可以直接使用向量检索结果：

- query 命中高频问题的稳定缓存；
- query 包含唯一文档 ID、产品型号或明确的关键词；
- 候选结果的相似度差距明显，且离线评估证明 Rerank 收益很小；
- 低延迟场景允许使用稍低的排序精度。

跳过策略必须可观测，记录 `rerank_skipped_reason`，并定期比较跳过与不跳过的 Recall、答案忠实度和用户反馈。

## 7. 第四层：LLM 生成优化

生成阶段经常是总耗时最大的部分。优化重点是减少输入和输出 token，并缩短用户等待首 token 的时间。

### 7.1 限制上下文长度

送入模型的 K 越大，输入 token 越多，TTFT 往往越高。上下文越长，也更容易把无关信息带入 Prompt。

可执行的控制包括：

- 限制 `context_k`；
- 为每个证据设置最大 token 数；
- 先按父文档去重，再截取相邻片段；
- 删除重复标题、重复页眉页脚和无关格式；
- 对超长证据先摘要，再保留原文引用 ID；
- 设置 Prompt 的总 token 上限，超限时按明确策略裁剪。

裁剪策略不能只从字符串末尾截断。应保留标题、关键段落、表格表头、来源和证据 ID，并在 Trace 中记录实际送入模型的内容摘要。

### 7.2 推理服务加速

可以根据模型和基础设施选择 vLLM、TensorRT-LLM 或 TGI 等推理服务。基准测试应包含：

- TTFT；
- 完整回答耗时；
- tokens/s；
- 并发请求数；
- 输入和输出 token 数；
- GPU 利用率与显存；
- 超时和排队时间。

不要只用单请求 benchmark 选型。单请求最快的服务，在高并发下可能因为排队或显存压力变慢。

### 7.3 流式返回

流式返回可以让用户在模型生成完整答案之前看到首 token 和后续 token。它主要降低感知延迟：

```text
请求 -> 检索完成 -> 首 token -> token 1 -> token 2 -> ... -> 完成事件
                     ^
                    TTFT
```

流式接口仍需要处理：

- 检索或模型失败后的错误事件；
- 客户端断开时取消下游生成；
- 答案完成事件和引用元数据；
- 部分答案已经发送后，不能再用普通 HTTP 错误码表示失败。

### 7.4 高频问题的答案缓存

对高频且答案稳定的问题，可以直接缓存已经生成的答案、引用和版本信息。

这里的“答案缓存”是应用层缓存，不应与 Transformer 推理过程中的内部 KV cache 混为一谈。应用层缓存可以完全跳过 Embedding、检索、Rerank 和 LLM 生成；模型内部 KV cache 则依赖具体推理服务和 token 前缀复用机制。

答案缓存 key 应包含：

- 规范化后的 query；
- 租户、用户或权限范围；
- 知识库或 Collection 版本；
- Prompt 模板版本；
- LLM 模型版本；
- 语言和产品配置。

缓存值至少应包含答案、引用、生成时间、知识版本和过期时间。命中缓存前仍要校验当前用户是否可以看到这些引用。

## 8. 第五层：架构优化

当单机性能优化已经接近上限，瓶颈通常转向容量、并发和长尾。架构层优化要解决的是“更多请求同时到来时，系统仍然可用”。

### 8.1 向量库分片与水平扩容

可以按租户、部门、业务域、时间或哈希分片。选择分片键时要考虑：

- 查询是否通常只涉及一个分片；
- 权限条件是否天然对应分片边界；
- 热点租户是否会造成单分片过载；
- 跨分片查询是否需要并行 fan-out；
- 结果合并和排序是否会增加长尾。

```text
                  +----------------+
query + filters  | Query Router   |
        -------->+----------------+
             /          |          \
            v           v           v
       shard A      shard B      shard C
            \          |          /
             +---------+---------+
                       v
                  merge + topK
```

跨分片查询应设置并发上限、单分片超时和部分结果策略。不能让一个慢分片无限期阻塞整个请求。

### 8.2 排序模型分片

Reranker 也可以通过多个副本或 GPU 分片扩展。调度器应根据候选数量和模型负载进行路由，并暴露队列等待时间。很多时候用户看到的“Rerank 很慢”其实是排队慢，而不是单次推理慢。

### 8.3 多级缓存

缓存不应该只放在一个组件中，可以按计算阶段建立缓存：

```text
L1：进程内 query cache
  -> L2：共享 Embedding cache
  -> L3：召回结果 cache
  -> L4：答案 cache
  -> 未命中后执行完整 RAG
```

不同缓存的失效条件不同：

| 缓存 | 典型 key | 主要失效条件 |
|---|---|---|
| Query/Embedding | 规范化 query + 模型版本 | 模型或规范化规则变化 |
| 召回结果 | query + 权限 + Filter + 索引版本 | 文档、权限或索引变化 |
| Rerank 结果 | query + 候选 ID + Rerank 版本 | 候选或排序模型变化 |
| 答案 | query + 权限 + Prompt/LLM/知识版本 | 数据、权限、模板或模型变化 |

缓存层越多，命中越快，但一致性越复杂。所有缓存 key 都要包含安全边界和版本信息，不能只用原始 query。

### 8.4 内存优先与持久化边界

热索引、常用 Payload 和高频答案可以放在内存或本地高速缓存中，但持久化数据、失效策略和重建流程必须保留。

“能不落盘就不落盘”只适用于可重新生成的临时数据或明确允许丢失的缓存，不适用于企业知识、权限元数据和索引版本记录。

## 9. 第六层：预打分快速通道

完整 RAG 的每一步都很有价值，但不是每个 query 都需要走完整链路。对于高频问题或包含明确关键词的 query，可以使用一个受约束的快速通道。

### 9.1 快速通道的目标

快速通道的目标是：

1. 尽早判断 query 是否可预测、可安全复用。
2. 命中时跳过不必要的 Embedding、向量检索或 Rerank。
3. 未命中或置信度不足时，透明地回退到完整 RAG。

```text
query
  -> 权限和规范化
  -> 高频问题/明确关键词判断
       | 命中且版本有效：快速答案
       | 否则：完整 RAG
```

### 9.2 预计算 `match_score` 与 `authority_score`

在数据入库时，可以为每条候选内容预计算可复用的匹配特征和权威性特征，并保存到 Payload：

```json
{
  "match_score": 0.82,
  "authority_score": 0.95,
  "score_version": "fast-path-v1",
  "source_version": "knowledge-2026-09-01"
}
```

最终快速通道分数可以定义为：

```text
fast_score = 0.4 * match_score + 0.6 * authority_score
```

这里的 `authority_score` 可以反映来源等级、文档审核状态、更新时间或业务负责人确认状态；`match_score` 可以来自关键词、实体、问题模板或规则匹配。

需要明确一个边界：任意新 query 的语义相似度通常依赖 query 向量，不能在入库时为所有未来 query 预先算完。预计算适合的是高频问题模板、关键词匹配、实体匹配等可复用特征；真正依赖 query 的语义 match，仍应由在线检索计算。

### 9.3 高频问题匹配

高频问题可以维护为经过审核的模板：

```json
{
  "query_template": "如何重置企业账号密码？",
  "answer": "请在账号设置中选择重置密码，并完成二次验证。",
  "citations": ["account-help-42"],
  "tenant_scope": "public",
  "knowledge_version": "v17",
  "expires_at": "2026-10-01T00:00:00Z"
}
```

匹配时应先做规范化和权限校验，再使用精确匹配、别名、关键词或小型分类器判断。模糊命中但没有足够置信度时，必须回退完整 RAG。

### 9.4 快速通道伪代码

```python
async def answer(query: str, auth: AuthContext) -> Answer:
    normalized = normalize_query(query)

    cached = await answer_cache.get(
        key=(normalized, auth.scope, KNOWLEDGE_VERSION, PROMPT_VERSION)
    )
    if cached and cached.is_valid_for(auth):
        return cached.answer

    fast_match = await fast_path.match(normalized, auth)
    if fast_match and fast_match.confidence >= 0.95:
        if fast_match.source_version == KNOWLEDGE_VERSION:
            return await generate_or_return_curated_answer(fast_match)

    query_vector = await embed(normalized)
    candidates = await vector_search(
        query_vector=query_vector,
        filters=auth.to_metadata_filter(),
        limit=CANDIDATE_K,
    )
    ranked = await maybe_rerank(normalized, candidates)
    context = select_context(ranked, limit=CONTEXT_K)
    return await stream_grounded_answer(normalized, context)
```

快速通道的关键不是“永远跳过 RAG”，而是让跳过行为具备三个条件：可解释、可回退、可失效。

### 9.5 快速通道的安全检查

每次命中快速通道都要检查：

- 当前用户、租户和部门是否有权访问答案及其引用；
- 答案引用的文档版本是否仍然有效；
- 内容是否已经被删除、撤回或标记为过期；
- `score_version` 和规则版本是否与在线服务兼容；
- query 是否可能包含要求不同答案的限定条件；
- 没有证据时是否会回退，而不是生成看似可信的固定答案。

## 10. 企业级标准链路

一个适合生产环境的标准在线架构可以收敛为：

```text
+------------------+
| 1. Cache Check   |
+------------------+
          |
          v
+------------------+       命中：直接返回或流式返回缓存答案
| 2. Embedding     |--------------------------------------+
+------------------+                                      |
          |                                               |
          v                                               |
+------------------+                                      |
| 3. Metadata      |                                      |
|    Filtering     |                                      |
+------------------+                                      |
          |                                               |
          v                                               |
+------------------+                                      |
| 4. Vector Search |                                      |
+------------------+                                      |
          |                                               |
          v                                               |
+------------------+                                      |
| 5. Lightweight   |                                      |
|    Rerank        |                                      |
+------------------+                                      |
          |                                               |
          v                                               |
+------------------+                                      |
| 6. Select topK   |                                      |
+------------------+                                      |
          |                                               |
          v                                               |
+------------------+                                      |
| 7. Stream Answer |<-------------------------------------+
+------------------+
```

其中第 5 步可以在命中明确规则、候选排序稳定或延迟预算不足时跳过；第 1 步缓存命中也不能绕过权限校验。

## 11. 端到端性能实验

### 实验 1：建立延迟基线

目标：知道每个阶段花了多少时间。

步骤：

1. 准备一组包含高频、长 query、权限过滤和无结果场景的固定查询。
2. 为每次请求生成 `trace_id`。
3. 记录缓存、Embedding、检索、Rerank、Prompt、TTFT 和完整答案耗时。
4. 统计 p50、p95、p99，而不是只统计平均值。
5. 找出占总 p95 延迟最高的两个阶段。

交付物：一张延迟瀑布图，以及每个阶段的目标预算。

### 实验 2：调优 HNSW 与 K

目标：找到 Recall 和延迟的可接受平衡。

步骤：

1. 固定 Embedding 模型和数据集。
2. 逐组测试多个 `ef`。
3. 对每个 `ef` 测试多个 `candidate_k`。
4. 记录 Recall@K、MRR、p95 检索延迟和总延迟。
5. 选择满足质量门槛的最低成本组合。

交付物：实验矩阵和最终配置理由。不能只写“ef 越大越准确”，必须给出数据。

### 实验 3：比较 Rerank 策略

目标：判断 Rerank 是否值得在线保留。

比较以下方案：

- 不使用 Rerank；
- 使用小模型逐条排序；
- 使用小模型 Batch 排序；
- 只在低置信度 query 上排序；
- 使用领域微调模型。

记录相关证据进入 `context_k` 的比例、Faithfulness、TTFT、Rerank p95 和 GPU 使用率。

### 实验 4：实现多级缓存

目标：验证缓存命中对延迟和质量的影响。

分别建立 query/Embedding、召回结果和答案缓存，并测试：

1. 冷启动请求；
2. 相同用户重复请求；
3. 不同用户相同 query；
4. 权限范围变化；
5. 知识库版本变化；
6. 文档删除或权限撤回后再次请求。

验收标准：缓存命中不会泄露跨租户答案，版本变化后不会继续返回旧证据。

### 实验 5：快速通道回退

目标：让快速路径只处理它真正理解的 query。

准备三类 query：

- 可以精确命中的高频问题；
- 包含明确实体或关键词的问题；
- 需要组合多个文档或复杂推理的问题。

验证：第一类命中快速通道，第二类只在置信度足够时命中，第三类必须进入完整 RAG。人为修改知识版本，确认快速答案失效并回退。

## 12. 评估指标与上线门槛

### 12.1 质量指标

- Recall@K：正确证据是否出现在候选中；
- MRR：第一个相关证据的排名；
- Context Precision：最终上下文中真正相关内容的比例；
- Faithfulness：答案是否被证据支持；
- Citation Accuracy：引用是否确实支持对应陈述；
- Filter Correctness：是否始终排除未授权数据；
- Fast-path fallback accuracy：快速通道拒绝或回退是否正确。

### 12.2 性能指标

- Cache hit rate：按缓存类型分别统计；
- Embedding p50/p95/p99；
- 向量检索 p50/p95/p99；
- Rerank 排队时间和推理时间；
- TTFT；
- 完整回答耗时；
- tokens/s；
- 每秒请求数和并发数；
- GPU 利用率、显存、CPU 和内存；
- 超时、取消和部分失败比例。

### 12.3 上线门槛示例

具体数值必须根据业务 SLA 设定，下面只提供定义方式：

```text
质量：Recall@candidate_k 不低于基线 - X 个百分点
权限：未授权证据泄露数 = 0
感知：TTFT p95 不超过 Y ms
完整：总耗时 p95 不超过 Z ms
稳定：超时率不超过 W%
成本：每千次请求平均 GPU/Token 成本不超过预算
```

如果速度变快但 Recall、Faithfulness 或权限正确性低于门槛，不能把这次实验标记为成功。

## 13. 常见错误与排查顺序

### 13.1 只优化向量库

如果 Embedding 占用了大部分时间，调 HNSW 不会改善总延迟。先看 Trace 中每个阶段的占比。

### 13.2 把 K 一味调小

K 变小通常会变快，但可能漏掉必要证据。应同时比较 Recall@K、最终答案质量和上下文 token 数。

### 13.3 用答案缓存绕过权限

同一个 query 在不同用户、租户、部门下可能对应不同答案。权限范围必须进入 cache key，并在命中时再次校验。

### 13.4 把所有 query 都送入快速通道

固定答案适合稳定 FAQ，不适合实时库存、权限变化、版本差异或需要多个文档联合判断的问题。置信度不足就回退。

### 13.5 把流式返回当成真正的计算加速

流式主要改善 TTFT 和用户感知。要减少总计算量，仍需减少输入 token、输出 token 或模型推理成本。

### 13.6 只看单并发 benchmark

生产问题经常出现在队列等待、连接池耗尽、GPU 显存不足、跨分片 fan-out 和缓存击穿。必须测试目标并发与突发流量。

### 13.7 缓存击穿和雪崩

热点 key 同时过期会导致大量请求一起执行 Embedding、检索和生成。可以使用 single-flight、随机 TTL、预热和限流，且要记录等待队列长度。

## 14. 生产检查清单

### 观测

- [ ] 每个阶段都有独立耗时和错误指标。
- [ ] 请求、缓存、检索、生成通过 `trace_id` 关联。
- [ ] 同时观察 p50、p95、p99。
- [ ] 能区分 cache miss、无相关数据、权限过滤后为空和下游失败。

### Embedding 与检索

- [ ] 文档和 query 的模型、维度、距离函数、Named Vector 一致。
- [ ] 高频 query 有带版本的向量缓存。
- [ ] HNSW `ef` 经过 Recall 与延迟实验。
- [ ] 热索引已预加载，readiness 不会早于预加载完成。
- [ ] 权限和元数据过滤在检索阶段生效。
- [ ] `candidate_k` 与 `context_k` 分开配置。

### 排序与生成

- [ ] Rerank 有“不使用”的基线。
- [ ] Rerank 支持批处理或明确的并发上限。
- [ ] Prompt 有总 token 上限和可解释的裁剪策略。
- [ ] 生产推理服务已测量 TTFT、tokens/s 和并发容量。
- [ ] 流式接口可以传递完成、取消和错误状态。
- [ ] 答案缓存包含权限、知识版本、Prompt 版本和模型版本。

### 快速通道与架构

- [ ] 快速通道的命中条件、分数版本和回退原因可观测。
- [ ] 预计算的 `match_score` 只用于可复用特征，不冒充任意 query 的语义分数。
- [ ] 快速答案命中前仍执行权限和版本检查。
- [ ] 向量库和 Reranker 的分片策略经过热点与跨分片测试。
- [ ] 缓存失效、文档删除和权限撤回有自动化测试。
- [ ] 缓存击穿、下游超时和部分分片失败有降级策略。

## 15. 总结

企业级 RAG 的性能不是某一个组件的开关，而是一条完整链路的预算管理：

```text
Embedding 更快
    -> 检索范围更小
    -> Rerank 候选更少
    -> Prompt 更短
    -> LLM 更早返回
    -> 缓存与扩容承接并发
```

最实用的标准路径是：

```text
缓存检查
  -> Embedding
  -> 元数据过滤
  -> 向量检索
  -> 轻量级 Rerank
  -> 选择最终 topK
  -> 流式返回
```

高频问题和明确关键词可以走预打分快速通道，但必须满足权限正确、版本有效、置信度足够，并且能够回退到完整 RAG。最终的优化判断应同时回答三个问题：

1. 用户是否更快看到答案？
2. 答案是否仍然由正确证据支持？
3. 系统是否能在真实并发和故障下稳定运行？

只有三个问题都得到肯定回答，才是企业级 RAG 的性能优化。

